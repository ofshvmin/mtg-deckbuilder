// Type declaration for the Tailwind entry stylesheet (`global.css`), imported for its
// side effects in app/_layout.tsx. Paired with `allowArbitraryExtensions` in tsconfig so
// `tsc` can resolve the relative `.css` import. NativeWind processes the CSS at build time.
export {};
